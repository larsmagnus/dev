# pow

A sample commander library.

## Testing

### Add the `pow` binary globally

```sh
cd ~/dev/packages/pow
pnpm link --global
```

### Remove the `pow` binary globally

```sh
cd ~/dev/packages/pow
pnpm unlink --global
```
